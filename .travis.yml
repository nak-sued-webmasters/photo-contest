language: node_js
node_js:
- node

install:
- npm install

before_script: 

script: 

deploy:
- provider: surge
  skip_cleanup: true
  project: "./"
  domain: nak-sued-fotocontest.surge.sh
  on:
    branch: develop

after_success:
    "curl --ftp-create-dirs -T uploadfilename -u $FTP_USER:$FTP_PASS ftp://fotocontest.nak-sued.de/web/"
